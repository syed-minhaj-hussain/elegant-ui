(function getResolution() {
    let resolution = `${window.screen.width} x ${window.screen.height}`;
    let width = window.screen.width;
    if(width <= 770){
        alert(`want to view the documentation code? Then Switch On the Desktop mode`)
    }
})();